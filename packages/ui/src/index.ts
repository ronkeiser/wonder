/**
 * @wonder/ui
 *
 * Opinionated UI components built on @wonder/components, @wonder/forms, and @wonder/icons.
 * Import the theme CSS to get default styling, then override variables as needed.
 *
 * @example
 * ```ts
 * // In your app's entry point
 * import '@wonder/ui/theme.css';
 * ```
 */

// Re-export everything from underlying packages
export * from '@wonder/components';
export * from '@wonder/forms';
export * from '@wonder/icons';
