{
  "name": "api",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "db:generate": "drizzle-kit generate",
    "db:migrate:local": "wrangler d1 migrations apply wonder --local",
    "db:migrate:remote": "wrangler d1 migrations apply wonder --remote",
    "db:studio": "drizzle-kit studio",
    "test": "vitest --run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "e2e:workflow": "tsx test/e2e/manual-workflow-test.ts",
    "e2e:websocket": "tsx test/e2e/manual-websocket-test.ts"
  },
  "dependencies": {
    "@wonder/logger": "workspace:*",
    "@wonder/schema": "workspace:*",
    "@wonder/sdk": "workspace:*",
    "drizzle-orm": "^0.44.7",
    "ulid": "^3.0.1"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.10.11",
    "@types/node": "^24.10.1",
    "@vitest/ui": "^3.2.4",
    "drizzle-kit": "^0.31.7",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4",
    "wrangler": "^4.50.0"
  }
}
