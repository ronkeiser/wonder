{
  "name": "@wonder/coordinator",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types -c wrangler.jsonc -c ../executor/wrangler.jsonc -c ../resources/wrangler.jsonc",
    "test": "vitest"
  },
  "dependencies": {
    "@wonder/logger": "workspace:*",
    "@wonder/schema": "workspace:*",
    "ulid": "^3.0.2"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.10.11",
    "wrangler": "^4.51.0"
  }
}
