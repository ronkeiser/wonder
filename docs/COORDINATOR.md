Read these docs in order:

1. `docs/architecture/branching.md`
2. `docs/architecture/coordinator.md`

Then review the current state of coordinator:

3. `services/coordinator/src/coordinator.ts`

Then review the current state of the edge test:

4. `packages/test/src/tests/edge.test.ts`
